# if field is default, it's can't be required = True:
#           default = <VALUE>    ->     required = False
#           default =   <>       ->     required = True
# EXAMPLES:
# input: int 50
# input: int
#
# text(max_len)


api/:
    v1/:
        delete-me:
            view: delete_me

        login:
            view: login
            input:
                access_token: text(300)

        logout:
            view: logout
